# The directory Mix will write compiled artifacts to.
/_build/
/lib/auth_manager_core/
/installer/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/



# Where 3rd-party dependencies like ExDoc and Swagger output generated docs.
/doc/
/priv/static/swagger.json

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
rephi-*.tar

mix.lock
package-lock.json

.claude/
CLAUDE.md
/apps/dashboard/CLAUDE.md

# Environment variables
.env
.env.local
.env.*.local
.yarn/
yarn.lock

# Frontend builds
/priv/static/dashboard/
/priv/static/admin/
/priv/static/ecommerce/
/priv/static/landing/

# Frontend node_modules and build artifacts
/apps/*/node_modules/
/apps/*/dist/
/apps/*/.vite/

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
pending.txt


# Ignore node_modules in shared components
/node_modules/
/packages/shared-components/node_modules/
/packages/shared-components/dist/
